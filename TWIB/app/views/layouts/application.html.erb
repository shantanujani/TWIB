<!DOCTYPE html>
<html>
<head>
  <title>TWIB</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

 <header>
    <h1>TWIB</h1>
    <nav>

  <% if session["user_id"].present? %>
      <%= link_to "Games", '/games' %> |
      <%= link_to "Teams", '/teams' %> |
      <%= link_to "Bets", '/bets' %> |
      <%= link_to "My results", '/results' %> |
      <% if User.find_by_id(session["user_id"]).commissioner == "Site Admin" %>
        <%= link_to "Users", "/users" %>
    <% end %>
      Hello, <%= User.find_by_id(session["user_id"]).username %>!
      <%= link_to "Sign Out", "/logout" %>
  <% else %>
      <%= link_to "Sign Up", '/users/new' %>
      <%= link_to "Login", '/login' %>
  <% end %>




  </header>


<div class="notice"><%= notice %></div>

<%= yield %>

</body>
</html>
