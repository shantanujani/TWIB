<h3>Users</h3>
<p><%= link_to 'Add a New User', new_user_url %></p>


<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>



<tbody>
<% @users.each do |user| %>
  <tr>
    <td><%= user.username%></td>
    <td><%= link_to 'Show', user_url(user) %></td>
    <td><%= link_to 'Edit', edit_user_url(user) %></td>
    <td><%= link_to 'Destroy', user_url(user), method: 'delete', data: { confirm: "Do you really want to delete this team?" } %></td>
  </tr>
<% end %>
</tbody>
</table>





</tr>
<br />
